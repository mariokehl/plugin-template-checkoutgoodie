{% set minSuffix = '.min' %}
{% if ceresConfig.log.performanceLevel  == 'development' %}
    {% set minSuffix = '' %}
{% endif %}

<link rel="stylesheet" href="{{ plugin_path('CheckoutGoodie') }}/css/style{{ minSuffix }}.css">